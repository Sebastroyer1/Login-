CREATE TABLE CCEMPLEADO_ROL (
    EMPLEADO_ID     NUMBER  NOT NULL,
    ROL_ID          NUMBER  NOT NULL,
    CONSTRAINT PK_CCEMPLEADO_ROL PRIMARY KEY (EMPLEADO_ID, ROL_ID),
    CONSTRAINT FK_EMPRОЛ_EMPLEADO FOREIGN KEY (EMPLEADO_ID) REFERENCES CCEMPLEADO(ID),
    CONSTRAINT FK_EMPROL_ROL      FOREIGN KEY (ROL_ID)      REFERENCES CCROL(ID)
);